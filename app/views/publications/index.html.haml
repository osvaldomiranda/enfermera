.container{:role => "main"}
  .col-md-12
    .panel.panel-default
      .panel-heading
        %h3.panel-title Noticias
      .panel-body
        .container-fluid
          .table-responsive
            %table.table.table-striped
              %thead
                %tr
                  %th Titulo
                  %th Resumen
                  %th
                  - if current_user.present?
                    %th
                    %th

              %tbody
                - @publications.each do |publication|
                  %tr
                    %td= publication.titulo
                    %td= publication.resumen
                    %td= link_to 'Ver', publication
                    - if current_user.present?
                      %td= link_to 'Edit', edit_publication_path(publication)
                      %td= link_to 'Destroy', publication, :method => :delete, :data => { :confirm => 'Are you sure?' }

            %br
            - if current_user.present?
              = link_to 'Crear Publicación', new_publication_path
